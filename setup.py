from setuptools import setup, find_packages

setup(
    name='webcam_control', # パッケージ名
    version='0.1',                      # バージョン番号
    packages=find_packages(),           # パッケージを自動的に探してインクルード
)
